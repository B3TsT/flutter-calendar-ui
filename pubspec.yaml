name: mobile_app_calendar
description: A new Flutter project.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  country: ^2.0.6
  country_provider2: ^1.0.2+4
  device_preview: ^1.1.0
  faker: ^2.1.0
  flutter:
    sdk: flutter
  intl: ^0.17.0
  timezone: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
